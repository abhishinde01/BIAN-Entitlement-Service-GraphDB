namespace EntitlementService.Models;

/// <summary>
/// Represents a customer or user identity within the BIAN Party Reference Data domain.
/// </summary>
public record Identity(string Id, string Name);
